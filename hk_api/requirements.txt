# 위성 텔레메트리 API 런타임(core/coordinates.py, core/loader 등)은 numpy/scipy 없이
# 자체 구현으로 동작. 다만 tests/test_loader.py(교차검증용)와 core/dynamics/*(로켓
# 발사체 GNC 시뮬레이션 - MATLAB 원본 포팅)는 numpy/scipy를 직접 사용하므로
# 테스트 실행 및 CI를 위해 여기 포함한다.
#
# sgp4 명시적 예외: core/propagation.py의 TLE 궤도 전파에 사용. SGP4는 WGS-72
# vallado 모델을 기반으로 사용
# httpx는 테스트 전용: fastapi.testclient.TestClient(starlette 기반)가 내부적으로 요구.
# 런타임에는 사용 안됨. 

fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.13.4
pydantic-settings==2.15.0
pandas==3.0.5
sqlalchemy==2.0.35
pymysql==1.1.1
pytest==8.3.3
numpy==2.5.2
scipy==1.18.1
sgp4==2.27
httpx==0.27.2
tomli==2.2.1; python_version < "3.11"